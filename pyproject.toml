[project]
name = "shabbat-print"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Reuven Lerner", email = "reuven@lerner.co.il" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
shabbat-print = "shabbat_print:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.6",
]
